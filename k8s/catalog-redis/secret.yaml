apiVersion: v1
kind: Secret
metadata:
  name: catalog-redis-secret
  namespace: fiap-cloud-games
type: Opaque
stringData:
  # Credencial academica local; use secret manager e rotacao em producao.
  FCG_REDIS_PASSWORD: fcg-local-redis
