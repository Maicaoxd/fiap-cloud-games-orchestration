apiVersion: v1
kind: Service
metadata:
  name: catalog-redis
  namespace: fiap-cloud-games
spec:
  type: ClusterIP
  selector:
    app: catalog-redis
  ports:
    - name: redis
      port: 6379
      targetPort: redis
