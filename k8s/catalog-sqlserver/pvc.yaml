apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: catalog-sqlserver-pvc
  namespace: fiap-cloud-games
  labels:
    app: catalog-sqlserver
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
