apiVersion: v1
kind: Service
metadata:
  name: catalog-api
  namespace: fiap-cloud-games
  labels:
    app: catalog-api
spec:
  type: ClusterIP
  selector:
    app: catalog-api
  ports:
    - name: http
      port: 80
      targetPort: http
