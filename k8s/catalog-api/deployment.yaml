apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-api
  namespace: fiap-cloud-games
  labels:
    app: catalog-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catalog-api
  template:
    metadata:
      labels:
        app: catalog-api
    spec:
      containers:
        - name: catalog-api
          image: maicaoxd/fiap-cloud-games-catalog-api:0.4.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: catalog-api-configmap
            - secretRef:
                name: catalog-api-secret
          env:
            - name: Redis__Password
              valueFrom:
                secretKeyRef:
                  name: catalog-redis-secret
                  key: FCG_REDIS_PASSWORD
            - name: MongoDb__Password
              valueFrom:
                secretKeyRef:
                  name: catalog-mongodb-secret
                  key: FCG_MONGODB_PASSWORD
          startupProbe:
            httpGet:
              path: /health/live
              port: http
            failureThreshold: 60
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
