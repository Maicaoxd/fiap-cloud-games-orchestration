apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: users-sqlserver-pvc
  namespace: fiap-cloud-games
  labels:
    app: users-sqlserver
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
