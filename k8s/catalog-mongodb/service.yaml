apiVersion: v1
kind: Service
metadata:
  name: catalog-mongodb
  namespace: fiap-cloud-games
spec:
  type: ClusterIP
  selector:
    app: catalog-mongodb
  ports:
    - name: mongodb
      port: 27017
      targetPort: mongodb
