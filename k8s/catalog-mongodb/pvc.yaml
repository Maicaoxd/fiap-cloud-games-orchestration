apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: catalog-mongodb-pvc
  namespace: fiap-cloud-games
  labels:
    app: catalog-mongodb
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
